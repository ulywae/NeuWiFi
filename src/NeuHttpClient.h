#ifndef NEU_HTTP_CLIENT_H
#define NEU_HTTP_CLIENT_H

#if !defined(ESP32)
#error "Platform not supported! NeuHttpClient currently only supports ESP32."
#endif

#include "http/_NeuHttpClient.h"

#endif