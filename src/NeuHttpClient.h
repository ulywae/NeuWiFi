#ifndef NEU_HTTP_CLIENT
#define NEU_HTTP_CLIENT

#if !defined(ESP32)
#error "Platform not supported! NeuHttpClient currently only supports ESP32."
#endif

#include "http/httpClient/httpClient.h"

#endif